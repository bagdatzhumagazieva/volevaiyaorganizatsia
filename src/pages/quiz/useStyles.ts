import { makeStyles, Theme, createStyles } from "@mui/material";

export const useStyles = makeStyles((theme: Theme) =>
    createStyles({
        form: {
            marginTop: '24px',
        }
    })
);
